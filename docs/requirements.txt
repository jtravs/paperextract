# Sphinx toolchain for the manual on Read the Docs; paperextract itself is
# installed separately by .readthedocs.yaml. Keep these in step with the
# dev dependency group in pyproject.toml.
sphinx>=8
myst-parser>=4
numpydoc>=1.9
pydata-sphinx-theme>=0.16
