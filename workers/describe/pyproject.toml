[project]
name = "paperextract-trial-describe"
version = "0.0.0"
requires-python = "==3.12.*"
dependencies = [
    "mlx-vlm==0.7.2",
]

[tool.uv]
package = false
environments = ["sys_platform == 'darwin' and platform_machine == 'arm64'"]
