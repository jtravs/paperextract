[project]
name = "paperextract-trial-marker"
version = "0.0.0"
requires-python = "==3.12.*"
dependencies = [
    "marker-pdf==2.0.0",
]

[tool.uv]
package = false
environments = [
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
]
